services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: chat-repo-pg
    environment:
      POSTGRES_PASSWORD: devpassword
      POSTGRES_DB: chatrepo
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
